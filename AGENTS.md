# AGENTS.md — راهنمای عامل‌های هوش مصنوعی برای این پروژه

این فایل مقصد اول هر ابزار/عامل هوش مصنوعی (Claude Code یا مشابه) است که روی این مخزن کار می‌کند. قبل از نوشتن یا تغییر هر کدی، این فایل و مستندات مرتبط را بخوان.

## چرا مستندسازی در این پروژه حیاتی است

این پروژه (شبیه‌سازی Quire) به‌عمد **پرجزئیات** است: ده‌ها صفحه، چند نمای مختلف تسک (Tree/Board/Table/Timeline/Calendar)، چند سطح از موجودیت‌ها (Organization → Project → Task → Subtask) و چند ویجت گزارشی تکرارشونده (Activity Heatmap، Pie Chart، Project Health). بدون مستندسازی دقیق، ارتباط بین بخش‌ها به‌سرعت گم می‌شود و هر عامل هوش مصنوعی که بعداً وارد پروژه شود مجبور است دوباره کل کد را کاوش کند تا بفهمد یک ماژول به چه چیزی وابسته است.

به همین دلیل، **مستندسازی به‌اندازه‌ی خود کد اهمیت دارد** و بخشی جدایی‌ناپذیر از تعریف «تمام‌شدن» هر تغییر است.

## قانون اصلی: هر ماژول = یک پوشه‌ی درست‌نام‌گذاری‌شده + یک README.md

هر بار که یک ماژول، فیچر، اپ یا بخش جدید ساخته می‌شود:

1. آن را در پوشه‌ی درست و با نام درست قرار بده — مطابق ساختاری که در [docs/FRONTEND.md](./docs/FRONTEND.md) و [docs/BACKEND.md](./docs/BACKEND.md) تعریف شده (مثلاً یک فیچر فرانت جدید داخل `frontend/features/<name>/`، یک اپ جدید بک‌اند داخل `backend/apps/<name>/`). اگر ساختار مناسب برای چیزی که می‌سازی در این اسناد وجود ندارد، ابتدا محل درست را در سند مربوطه تصمیم‌گیری و ثبت کن، سپس کد را آنجا بنویس.
2. داخل همان پوشه یک فایل `README.md` کوتاه بنویس که شامل:
   - **این ماژول چه کاری انجام می‌دهد** (خلاصه‌ی یک تا چند خطی، نه توضیح خط‌به‌خط کد).
   - **به چه چیزی وابسته است** (کدام مدل‌های بک‌اند، کدام APIها، کدام کامپوننت/ماژول‌های دیگر فرانت).
   - **چه چیزی به آن وابسته است / چه کسی از آن استفاده می‌کند** (مثلاً «این کامپوننت توسط BoardView و TableView مشترکاً استفاده می‌شود»).
3. اگر تغییری در یک ماژول باعث تغییر رفتار/رابط آن برای مصرف‌کنندگانش می‌شود، `README.md` همان پوشه و در صورت نیاز اسناد بالادستی (`docs/`) باید در همان تغییر (همان commit/PR) به‌روزرسانی شوند — نه بعداً.

## ساختار مستندات پروژه

```
Quire/
  AGENTS.md              → همین فایل: قوانین کار عامل‌های هوش مصنوعی
  docs/
    README.md             → فهرست و ترتیب مطالعه‌ی مستندات
    PRODUCT_OVERVIEW.md    → منبع حقیقت محصول: مفاهیم، مدل داده‌ی مفهومی، شرح صفحات
    DESIGN.md              → زبان بصری: رنگ، تایپوگرافی، اندازه‌ها، الگوی کامپوننت‌ها
    FRONTEND.md            → معماری Next.js: مسیرها، کامپوننت‌ها، لایه‌ی داده
    BACKEND.md             → معماری Django + SQLite: اپ‌ها، مدل داده، API، مسیر مهاجرت
  frontend/                → کد Next.js (هر فیچر با README.md خودش)
  backend/                 → کد Django (هر اپ با README.md خودش)
```

قبل از هر کار، این ترتیب رعایت شود: `docs/PRODUCT_OVERVIEW.md` → `docs/DESIGN.md` یا سند تخصصی مرتبط (`FRONTEND.md`/`BACKEND.md`) → `README.md` پوشه‌ی همان ماژول (اگر موجود است) → کد.

## قوانین دیگر کار روی این پروژه

- **فاز فعلی پروژه**: فقط فرانت‌اند (Next.js) به‌طور کامل پیاده‌سازی می‌شود؛ بک‌اند (Django + SQLite) عمداً ساده و مبتدی نگه داشته می‌شود اما باید طبق اصول مهاجرت‌پذیری در [docs/BACKEND.md](./docs/BACKEND.md#۱-فلسفهی-فاز-اول) نوشته شود (فقط ORM، تنظیمات از env، مرز دامنه‌ی مشخص بین اپ‌ها). داده‌های واقعی در این فاز اهمیتی ندارند و قابل پاک‌سازی هستند؛ **ساختار** همیشه مهم‌تر از داده است.
- **طراحی بصری** همیشه باید با اصول [docs/DESIGN.md](./docs/DESIGN.md) هماهنگ باشد: بدون سایه، دکمه‌های Ghost برای اکشن‌های ثانویه، فاصله‌گذاری فشرده، تک‌رنگ به‌جز آیکون اولویت تسک، ارتفاع دکمه ۳۲/۳۶px، فونت پایه ۱۴px.
- هرگاه مفهوم محصول تغییر کند (مثلاً فیلد جدید روی Task، صفحه‌ی جدید)، ابتدا [docs/PRODUCT_OVERVIEW.md](./docs/PRODUCT_OVERVIEW.md) به‌روزرسانی شود، سپس `FRONTEND.md`/`BACKEND.md` و در نهایت کد.
- از افزودن انتزاع، فیچر یا پیکربندی «برای آینده‌ای که هنوز مشخص نیست» خودداری شود؛ فقط چیزی که در مستندات فعلی توضیح داده شده پیاده‌سازی شود.
